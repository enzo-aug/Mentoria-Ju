package com.example.projetomentoria.solid.lsp.rectangle

class User {
    fun calculateArea(rectangle: Rectangle) {
        println("A rea do retângulo é: ${rectangle.height * rectangle.width}")
    }
}